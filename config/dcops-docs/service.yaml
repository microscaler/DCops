apiVersion: v1
kind: Service
metadata:
  name: dcops-docs
  namespace: dcops-system
  labels:
    app: dcops-docs
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: dcops-docs
