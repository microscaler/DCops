apiVersion: apps/v1
kind: Deployment
metadata:
  name: ip-claim-controller
  namespace: dcops-system
  labels:
    app: ip-claim-controller
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ip-claim-controller
  template:
    metadata:
      labels:
        app: ip-claim-controller
    spec:
      serviceAccountName: ip-claim-controller
      containers:
      - name: ip-claim-controller
        image: ip-claim-controller
        imagePullPolicy: Always
        env:
        - name: NETBOX_URL
          value: "http://netbox.netbox:80"
        - name: NETBOX_TOKEN
          valueFrom:
            secretKeyRef:
              name: netbox-token
              key: token
        - name: WATCH_NAMESPACE
          value: "default"  # Watch specific namespace, or remove to watch all
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 256Mi

