# This file is auto-generated by crdgen
# DO NOT EDIT THIS FILE MANUALLY
# If there are malformed YAML issues, fix them in the Rust code (crates/crds/src/)
# This file will be overwritten on every code update
#
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: ipclaims.dcops.microscaler.io
spec:
  group: dcops.microscaler.io
  names:
    categories: []
    kind: IPClaim
    plural: ipclaims
    shortNames: []
    singular: ipclaim
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for IPClaimSpec via `CustomResource`
        properties:
          spec:
            properties:
              deviceRef:
                description: Device/interface reference
                properties:
                  interface:
                    description: Interface name (optional)
                    nullable: true
                    type: string
                  name:
                    description: Device name or identifier
                    type: string
                required:
                - name
                type: object
              poolRef:
                description: Reference to IPPool
                properties:
                  name:
                    description: Name of the IPPool
                    type: string
                  namespace:
                    description: Namespace (defaults to same namespace as IPClaim)
                    nullable: true
                    type: string
                required:
                - name
                type: object
              preferredIp:
                description: |-
                  Preferred IP (hint, not guarantee)
                  Must be a valid CIDR notation (IP address with prefix length, e.g., "192.168.1.10/24")
                nullable: true
                pattern: ^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.){3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)/(?:[0-9]|[12][0-9]|3[0-2])$|^(?:[0-9a-fA-F]{1,4}:){7}[0-9a-fA-F]{1,4}/(?:[0-9]|[1-9][0-9]|1[0-2][0-8])$
                type: string
            required:
            - deviceRef
            - poolRef
            type: object
          status:
            nullable: true
            properties:
              error:
                description: Error message if allocation failed
                nullable: true
                type: string
              ip:
                description: Allocated IP address
                nullable: true
                type: string
              lastReconciled:
                description: Last reconciliation timestamp
                format: date-time
                nullable: true
                type: string
              netboxIpRef:
                description: NetBox IPAddress object reference
                nullable: true
                type: string
              state:
                description: Allocation state
                enum:
                - Pending
                - Allocated
                - Failed
                type: string
            required:
            - state
            type: object
        required:
        - spec
        title: IPClaim
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: ippools.dcops.microscaler.io
spec:
  group: dcops.microscaler.io
  names:
    categories: []
    kind: IPPool
    plural: ippools
    shortNames: []
    singular: ippool
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for IPPoolSpec via `CustomResource`
        properties:
          spec:
            properties:
              allocationStrategy:
                default: sequential
                description: Allocation strategy
                enum:
                - sequential
                - random
                type: string
              netboxPrefixRef:
                description: |-
                  NetBox prefix reference (references NetBoxPrefix CRD)
                  This is a stable, GitOps-friendly reference that resolves to the NetBox prefix ID
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
              role:
                default: ''
                description: Pool scope/role (e.g., "control-plane", "worker", "management")
                type: string
            required:
            - netboxPrefixRef
            type: object
          status:
            nullable: true
            properties:
              allocatedIps:
                description: Allocated IPs
                format: uint32
                minimum: 0.0
                type: integer
              availableIps:
                description: Available IPs
                format: uint32
                minimum: 0.0
                type: integer
              lastReconciled:
                description: Last reconciliation timestamp
                format: date-time
                nullable: true
                type: string
              netboxPrefixId:
                description: |-
                  Resolved NetBox prefix ID (observed state)
                  This is set by the controller after resolving the NetBoxPrefix CRD reference
                format: uint64
                minimum: 0.0
                nullable: true
                type: integer
              netboxPrefixUrl:
                description: NetBox prefix URL
                nullable: true
                type: string
              totalIps:
                description: Total available IPs in pool
                format: uint32
                minimum: 0.0
                type: integer
            required:
            - allocatedIps
            - availableIps
            - totalIps
            type: object
        required:
        - spec
        title: IPPool
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: netboxprefixes.dcops.microscaler.io
spec:
  group: dcops.microscaler.io
  names:
    categories: []
    kind: NetBoxPrefix
    plural: netboxprefixes
    shortNames: []
    singular: netboxprefix
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for NetBoxPrefixSpec via `CustomResource`
        properties:
          spec:
            description: NetBoxPrefixSpec defines the desired state of a NetBox prefix
            properties:
              aggregate:
                description: Aggregate reference (references NetBoxAggregate CRD)
                nullable: true
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
              comments:
                description: Comments
                nullable: true
                type: string
              description:
                description: Description of the prefix
                nullable: true
                type: string
              prefix:
                description: |-
                  Prefix CIDR (e.g., "192.168.1.0/24" or "2001:db8::/64")
                  Must be a valid CIDR notation (IP address with prefix length)
                pattern: ^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.){3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)/(?:[0-9]|[12][0-9]|3[0-2])$|^(?:[0-9a-fA-F]{1,4}:){7}[0-9a-fA-F]{1,4}/(?:[0-9]|[1-9][0-9]|1[0-2][0-8])$
                type: string
              role:
                description: Role reference (references NetBoxRole CRD)
                nullable: true
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
              site:
                description: Site reference (references NetBoxSite CRD)
                nullable: true
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
              status:
                default: active
                description: Status in NetBox (active, reserved, deprecated, container)
                enum:
                - active
                - reserved
                - deprecated
                - container
                type: string
              tags:
                description: Tag references (references NetBoxTag CRDs)
                items:
                  description: |-
                    Kubernetes-compliant resource reference for NetBox CRDs

                    This follows the Kubernetes `TypedLocalObjectReference` pattern, which includes:
                    - `apiGroup`: The API group of the referenced resource (e.g., "dcops.microscaler.io")
                    - `kind`: The kind of the referenced resource (e.g., "NetBoxSite")
                    - `name`: The name of the referenced resource (required)
                    - `namespace`: The namespace of the referenced resource (optional, defaults to same namespace)

                    This enables Kubernetes to validate reference types and provides clear documentation
                    of what resource type is expected.
                  properties:
                    apiGroup:
                      description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                      type: string
                    kind:
                      description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                      type: string
                    name:
                      description: Name of the referenced NetBox CRD resource
                      type: string
                    namespace:
                      description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                      nullable: true
                      type: string
                  required:
                  - apiGroup
                  - kind
                  - name
                  type: object
                nullable: true
                type: array
              tenant:
                description: |-
                  Tenant reference (references NetBoxTenant CRD, required)
                  Tenant is required in NetBox for proper resource organization and access control
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
              vlan:
                description: VLAN reference (references NetBoxVLAN CRD)
                nullable: true
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
            required:
            - prefix
            - tenant
            type: object
          status:
            description: NetBoxPrefixStatus defines the observed state of a NetBox prefix
            nullable: true
            properties:
              error:
                description: Error message if reconciliation failed
                nullable: true
                type: string
              lastReconciled:
                description: Last reconciliation timestamp
                format: date-time
                nullable: true
                type: string
              netboxId:
                description: NetBox prefix ID (set after creation)
                format: uint64
                minimum: 0.0
                nullable: true
                type: integer
              netboxUrl:
                description: NetBox prefix URL
                nullable: true
                type: string
              state:
                description: Current state of the prefix
                enum:
                - Pending
                - Created
                - Updated
                - Failed
                type: string
            required:
            - state
            type: object
        required:
        - spec
        title: NetBoxPrefix
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: netboxtenants.dcops.microscaler.io
spec:
  group: dcops.microscaler.io
  names:
    categories: []
    kind: NetBoxTenant
    plural: netboxtenants
    shortNames: []
    singular: netboxtenant
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for NetBoxTenantSpec via `CustomResource`
        properties:
          spec:
            description: NetBoxTenantSpec defines the desired state of a NetBox tenant
            properties:
              comments:
                description: Comments
                nullable: true
                type: string
              description:
                description: Description of the tenant
                nullable: true
                type: string
              group:
                description: Tenant group reference (references NetBoxTenantGroup CRD, optional)
                nullable: true
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
              name:
                description: Tenant name
                type: string
              reconcileInterval:
                description: |-
                  Reconcile interval in seconds

                  How often to check for drift between the CRD spec and NetBox, even when no changes occur.
                  Defaults to 300 seconds (5 minutes) if not specified.
                  Set to 0 to disable periodic reconciliation (only reconcile on changes).
                format: uint64
                minimum: 0.0
                nullable: true
                type: integer
              slug:
                description: Tenant slug (optional, auto-generated from name if not provided)
                nullable: true
                type: string
              tags:
                description: Tag references (references NetBoxTag CRDs)
                items:
                  description: |-
                    Kubernetes-compliant resource reference for NetBox CRDs

                    This follows the Kubernetes `TypedLocalObjectReference` pattern, which includes:
                    - `apiGroup`: The API group of the referenced resource (e.g., "dcops.microscaler.io")
                    - `kind`: The kind of the referenced resource (e.g., "NetBoxSite")
                    - `name`: The name of the referenced resource (required)
                    - `namespace`: The namespace of the referenced resource (optional, defaults to same namespace)

                    This enables Kubernetes to validate reference types and provides clear documentation
                    of what resource type is expected.
                  properties:
                    apiGroup:
                      description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                      type: string
                    kind:
                      description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                      type: string
                    name:
                      description: Name of the referenced NetBox CRD resource
                      type: string
                    namespace:
                      description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                      nullable: true
                      type: string
                  required:
                  - apiGroup
                  - kind
                  - name
                  type: object
                nullable: true
                type: array
              tokenSecret:
                description: |-
                  Secret reference containing the NetBox API token for this tenant

                  The Secret must contain a key (defaults to "token") with the NetBox API token.
                  If namespace is not specified, the Secret is expected to be in the same namespace
                  as the Tenant CRD.
                properties:
                  key:
                    description: Key within the Secret data (optional, defaults to "token")
                    nullable: true
                    type: string
                  name:
                    description: Name of the Kubernetes Secret
                    type: string
                  namespace:
                    description: Namespace of the Secret (optional, defaults to the namespace of the referencing resource)
                    nullable: true
                    type: string
                required:
                - name
                type: object
            required:
            - name
            - tokenSecret
            type: object
          status:
            description: NetBoxTenantStatus defines the observed state of a NetBox tenant
            nullable: true
            properties:
              error:
                description: Error message if reconciliation failed
                nullable: true
                type: string
              lastReconciled:
                description: Last reconciliation timestamp
                format: date-time
                nullable: true
                type: string
              netboxId:
                description: NetBox tenant ID (set after creation)
                format: uint64
                minimum: 0.0
                nullable: true
                type: integer
              netboxUrl:
                description: NetBox tenant URL
                nullable: true
                type: string
              state:
                description: Current state of the tenant
                enum:
                - Pending
                - Created
                - Updated
                - Failed
                type: string
            required:
            - state
            type: object
        required:
        - spec
        title: NetBoxTenant
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: netboxregions.dcops.microscaler.io
spec:
  group: dcops.microscaler.io
  names:
    categories: []
    kind: NetBoxRegion
    plural: netboxregions
    shortNames: []
    singular: netboxregion
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for NetBoxRegionSpec via `CustomResource`
        properties:
          spec:
            description: NetBoxRegionSpec defines the desired state of a NetBox region
            properties:
              comments:
                description: Comments
                nullable: true
                type: string
              description:
                description: Description of the region
                nullable: true
                type: string
              name:
                description: Region name
                type: string
              parent:
                description: Parent region reference (references NetBoxRegion CRD for hierarchical organization)
                nullable: true
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
              slug:
                description: Region slug (optional, auto-generated from name if not provided)
                nullable: true
                type: string
              tags:
                description: Tag references (references NetBoxTag CRDs)
                items:
                  description: |-
                    Kubernetes-compliant resource reference for NetBox CRDs

                    This follows the Kubernetes `TypedLocalObjectReference` pattern, which includes:
                    - `apiGroup`: The API group of the referenced resource (e.g., "dcops.microscaler.io")
                    - `kind`: The kind of the referenced resource (e.g., "NetBoxSite")
                    - `name`: The name of the referenced resource (required)
                    - `namespace`: The namespace of the referenced resource (optional, defaults to same namespace)

                    This enables Kubernetes to validate reference types and provides clear documentation
                    of what resource type is expected.
                  properties:
                    apiGroup:
                      description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                      type: string
                    kind:
                      description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                      type: string
                    name:
                      description: Name of the referenced NetBox CRD resource
                      type: string
                    namespace:
                      description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                      nullable: true
                      type: string
                  required:
                  - apiGroup
                  - kind
                  - name
                  type: object
                nullable: true
                type: array
            required:
            - name
            type: object
          status:
            description: NetBoxRegionStatus defines the observed state of a NetBox region
            nullable: true
            properties:
              error:
                description: Error message if reconciliation failed
                nullable: true
                type: string
              lastReconciled:
                description: Last reconciliation timestamp
                format: date-time
                nullable: true
                type: string
              netboxId:
                description: NetBox region ID (set after creation)
                format: uint64
                minimum: 0.0
                nullable: true
                type: integer
              netboxUrl:
                description: NetBox region URL
                nullable: true
                type: string
              state:
                description: Current state of the region
                enum:
                - Pending
                - Created
                - Updated
                - Failed
                type: string
            required:
            - state
            type: object
        required:
        - spec
        title: NetBoxRegion
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: netboxsitegroups.dcops.microscaler.io
spec:
  group: dcops.microscaler.io
  names:
    categories: []
    kind: NetBoxSiteGroup
    plural: netboxsitegroups
    shortNames: []
    singular: netboxsitegroup
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for NetBoxSiteGroupSpec via `CustomResource`
        properties:
          spec:
            description: NetBoxSiteGroupSpec defines the desired state of a NetBox site group
            properties:
              description:
                description: Description of the site group
                nullable: true
                type: string
              name:
                description: Site group name
                type: string
              parent:
                description: Parent site group reference (references NetBoxSiteGroup CRD for hierarchical organization)
                nullable: true
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
              slug:
                description: Site group slug (optional, auto-generated from name if not provided)
                nullable: true
                type: string
              tags:
                description: Tag references (references NetBoxTag CRDs)
                items:
                  description: |-
                    Kubernetes-compliant resource reference for NetBox CRDs

                    This follows the Kubernetes `TypedLocalObjectReference` pattern, which includes:
                    - `apiGroup`: The API group of the referenced resource (e.g., "dcops.microscaler.io")
                    - `kind`: The kind of the referenced resource (e.g., "NetBoxSite")
                    - `name`: The name of the referenced resource (required)
                    - `namespace`: The namespace of the referenced resource (optional, defaults to same namespace)

                    This enables Kubernetes to validate reference types and provides clear documentation
                    of what resource type is expected.
                  properties:
                    apiGroup:
                      description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                      type: string
                    kind:
                      description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                      type: string
                    name:
                      description: Name of the referenced NetBox CRD resource
                      type: string
                    namespace:
                      description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                      nullable: true
                      type: string
                  required:
                  - apiGroup
                  - kind
                  - name
                  type: object
                nullable: true
                type: array
            required:
            - name
            type: object
          status:
            description: NetBoxSiteGroupStatus defines the observed state of a NetBox site group
            nullable: true
            properties:
              error:
                description: Error message if reconciliation failed
                nullable: true
                type: string
              lastReconciled:
                description: Last reconciliation timestamp
                format: date-time
                nullable: true
                type: string
              netboxId:
                description: NetBox site group ID (set after creation)
                format: uint64
                minimum: 0.0
                nullable: true
                type: integer
              netboxUrl:
                description: NetBox site group URL
                nullable: true
                type: string
              state:
                description: Current state of the site group
                enum:
                - Pending
                - Created
                - Updated
                - Failed
                type: string
            required:
            - state
            type: object
        required:
        - spec
        title: NetBoxSiteGroup
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: netboxlocations.dcops.microscaler.io
spec:
  group: dcops.microscaler.io
  names:
    categories: []
    kind: NetBoxLocation
    plural: netboxlocations
    shortNames: []
    singular: netboxlocation
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for NetBoxLocationSpec via `CustomResource`
        properties:
          spec:
            description: NetBoxLocationSpec defines the desired state of a NetBox location
            properties:
              description:
                description: Description of the location
                nullable: true
                type: string
              facility:
                description: Facility identifier (optional but recommended for data center locations)
                nullable: true
                type: string
              name:
                description: Location name
                type: string
              parent:
                description: |-
                  Parent location reference (references NetBoxLocation CRD for nested locations)
                  If not provided, this is a top-level location (parent will be null in NetBox)
                nullable: true
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
              site:
                description: Site reference (references NetBoxSite CRD, required)
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
              slug:
                description: Location slug (optional, auto-generated from name if not provided)
                nullable: true
                type: string
              tags:
                description: Tag references (references NetBoxTag CRDs)
                items:
                  description: |-
                    Kubernetes-compliant resource reference for NetBox CRDs

                    This follows the Kubernetes `TypedLocalObjectReference` pattern, which includes:
                    - `apiGroup`: The API group of the referenced resource (e.g., "dcops.microscaler.io")
                    - `kind`: The kind of the referenced resource (e.g., "NetBoxSite")
                    - `name`: The name of the referenced resource (required)
                    - `namespace`: The namespace of the referenced resource (optional, defaults to same namespace)

                    This enables Kubernetes to validate reference types and provides clear documentation
                    of what resource type is expected.
                  properties:
                    apiGroup:
                      description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                      type: string
                    kind:
                      description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                      type: string
                    name:
                      description: Name of the referenced NetBox CRD resource
                      type: string
                    namespace:
                      description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                      nullable: true
                      type: string
                  required:
                  - apiGroup
                  - kind
                  - name
                  type: object
                nullable: true
                type: array
              tenant:
                description: |-
                  Tenant reference (references NetBoxTenant CRD, required)
                  Tenant is required in NetBox for proper resource organization and access control
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
            required:
            - name
            - site
            - tenant
            type: object
          status:
            description: NetBoxLocationStatus defines the observed state of a NetBox location
            nullable: true
            properties:
              error:
                description: Error message if reconciliation failed
                nullable: true
                type: string
              lastReconciled:
                description: Last reconciliation timestamp
                format: date-time
                nullable: true
                type: string
              netboxId:
                description: NetBox location ID (set after creation)
                format: uint64
                minimum: 0.0
                nullable: true
                type: integer
              netboxUrl:
                description: NetBox location URL
                nullable: true
                type: string
              state:
                description: Current state of the location
                enum:
                - Pending
                - Created
                - Updated
                - Failed
                type: string
            required:
            - state
            type: object
        required:
        - spec
        title: NetBoxLocation
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: netboxsites.dcops.microscaler.io
spec:
  group: dcops.microscaler.io
  names:
    categories: []
    kind: NetBoxSite
    plural: netboxsites
    shortNames: []
    singular: netboxsite
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for NetBoxSiteSpec via `CustomResource`
        properties:
          spec:
            description: NetBoxSiteSpec defines the desired state of a NetBox site
            properties:
              comments:
                description: Comments
                nullable: true
                type: string
              description:
                description: Description of the site
                nullable: true
                type: string
              facility:
                description: Facility
                nullable: true
                type: string
              latitude:
                description: Latitude
                format: double
                nullable: true
                type: number
              longitude:
                description: Longitude
                format: double
                nullable: true
                type: number
              name:
                description: Site name
                type: string
              physicalAddress:
                description: Physical address
                nullable: true
                type: string
              region:
                description: Region reference (references NetBoxRegion CRD, optional)
                nullable: true
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
              shippingAddress:
                description: Shipping address
                nullable: true
                type: string
              siteGroup:
                description: Site group reference (references NetBoxSiteGroup CRD, optional)
                nullable: true
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
              slug:
                description: Site slug (optional, auto-generated from name if not provided)
                nullable: true
                type: string
              status:
                default: active
                description: Status (active, planned, retired, staging)
                enum:
                - active
                - planned
                - retired
                - staging
                type: string
              tags:
                description: Tag references (references NetBoxTag CRDs)
                items:
                  description: |-
                    Kubernetes-compliant resource reference for NetBox CRDs

                    This follows the Kubernetes `TypedLocalObjectReference` pattern, which includes:
                    - `apiGroup`: The API group of the referenced resource (e.g., "dcops.microscaler.io")
                    - `kind`: The kind of the referenced resource (e.g., "NetBoxSite")
                    - `name`: The name of the referenced resource (required)
                    - `namespace`: The namespace of the referenced resource (optional, defaults to same namespace)

                    This enables Kubernetes to validate reference types and provides clear documentation
                    of what resource type is expected.
                  properties:
                    apiGroup:
                      description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                      type: string
                    kind:
                      description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                      type: string
                    name:
                      description: Name of the referenced NetBox CRD resource
                      type: string
                    namespace:
                      description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                      nullable: true
                      type: string
                  required:
                  - apiGroup
                  - kind
                  - name
                  type: object
                nullable: true
                type: array
              tenant:
                description: |-
                  Tenant reference (references NetBoxTenant CRD, required)
                  Tenant is required in NetBox for proper resource organization and access control
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
              timeZone:
                description: Time zone
                nullable: true
                type: string
            required:
            - name
            - tenant
            type: object
          status:
            description: NetBoxSiteStatus defines the observed state of a NetBox site
            nullable: true
            properties:
              error:
                description: Error message if reconciliation failed
                nullable: true
                type: string
              lastReconciled:
                description: Last reconciliation timestamp
                format: date-time
                nullable: true
                type: string
              netboxId:
                description: NetBox site ID (set after creation)
                format: uint64
                minimum: 0.0
                nullable: true
                type: integer
              netboxUrl:
                description: NetBox site URL
                nullable: true
                type: string
              state:
                description: Current state of the site
                enum:
                - Pending
                - Created
                - Updated
                - Failed
                type: string
            required:
            - state
            type: object
        required:
        - spec
        title: NetBoxSite
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: netboxipaddresses.dcops.microscaler.io
spec:
  group: dcops.microscaler.io
  names:
    categories: []
    kind: NetBoxIPAddress
    plural: netboxipaddresses
    shortNames: []
    singular: netboxipaddress
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for NetBoxIPAddressSpec via `CustomResource`
        properties:
          spec:
            description: NetBoxIPAddressSpec defines the desired state of a NetBox IP address
            properties:
              address:
                description: |-
                  IP address with CIDR (e.g., "192.168.1.10/24" or "2001:db8::1/64")
                  Must be a valid CIDR notation (IP address with prefix length)

                  For DHCP-assigned IPs:
                  - If tracking an already-assigned IP, specify the address here
                  - If using an IP range reference, address is optional (will be allocated from range)
                  - At least one of `address` or `ip_range` must be specified
                nullable: true
                pattern: ^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.){3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)/(?:[0-9]|[12][0-9]|3[0-2])$|^(?:[0-9a-fA-F]{1,4}:){7}[0-9a-fA-F]{1,4}/(?:[0-9]|[1-9][0-9]|1[0-2][0-8])$
                type: string
              comments:
                description: Comments
                nullable: true
                type: string
              description:
                description: Description
                nullable: true
                type: string
              dnsName:
                description: DNS name
                nullable: true
                type: string
              interface:
                description: |-
                  Interface reference (references NetBoxInterface CRD, optional)
                  For static DHCP reservations, this can be resolved from macAddress
                  If both macAddress and interface are specified, interface takes precedence
                  When specified, the IP address will be assigned to this interface in NetBox
                nullable: true
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
              ipRange:
                description: |-
                  IP range reference (references NetBoxIPRange CRD, optional)

                  For DHCP-assigned IPs, reference the IP range pool from which the IP was assigned.
                  This links the IP address to its DHCP pool for proper tracking and management.

                  If `ip_range` is specified and `address` is not, the reconciler will allocate
                  an IP from the range. If both are specified, the address must be within the range.
                nullable: true
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
              macAddress:
                description: |-
                  MAC address in hex format (e.g., "aa:bb:cc:dd:ee:ff" or "aa-bb-cc-dd-ee-ff")
                  Required for static DHCP reservations (status: dhcp with address specified)
                  Used to resolve the interface to which this IP should be assigned
                  If both macAddress and interface are specified, interface takes precedence
                nullable: true
                type: string
              role:
                description: IP address role (loopback, secondary, anycast, vip, vrrp, hsrp, glbp, carp)
                nullable: true
                type: string
              status:
                default: active
                description: IP address status (active, reserved, deprecated, dhcp, slaac)
                enum:
                - active
                - reserved
                - deprecated
                - dhcp
                - slaac
                type: string
              tags:
                description: Tag references (references NetBoxTag CRDs)
                items:
                  description: |-
                    Kubernetes-compliant resource reference for NetBox CRDs

                    This follows the Kubernetes `TypedLocalObjectReference` pattern, which includes:
                    - `apiGroup`: The API group of the referenced resource (e.g., "dcops.microscaler.io")
                    - `kind`: The kind of the referenced resource (e.g., "NetBoxSite")
                    - `name`: The name of the referenced resource (required)
                    - `namespace`: The namespace of the referenced resource (optional, defaults to same namespace)

                    This enables Kubernetes to validate reference types and provides clear documentation
                    of what resource type is expected.
                  properties:
                    apiGroup:
                      description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                      type: string
                    kind:
                      description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                      type: string
                    name:
                      description: Name of the referenced NetBox CRD resource
                      type: string
                    namespace:
                      description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                      nullable: true
                      type: string
                  required:
                  - apiGroup
                  - kind
                  - name
                  type: object
                nullable: true
                type: array
              tenant:
                description: |-
                  Tenant reference (references NetBoxTenant CRD, required)
                  Tenant is required in NetBox for proper resource organization and access control
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
              vlan:
                description: VLAN reference (references NetBoxVLAN CRD, optional)
                nullable: true
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
              vrf:
                description: VRF reference (references NetBoxVRF CRD, optional - not yet implemented)
                nullable: true
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
            required:
            - tenant
            type: object
          status:
            description: NetBoxIPAddressStatus defines the observed state of a NetBox IP address
            nullable: true
            properties:
              address:
                description: |-
                  IP address with CIDR (set after creation/reconciliation)
                  For DHCP-assigned IPs, this is the IP address that was allocated/assigned
                  and is stored in the status rather than the spec
                nullable: true
                type: string
              error:
                description: Error message if reconciliation failed
                nullable: true
                type: string
              lastReconciled:
                description: Last reconciliation timestamp
                format: date-time
                nullable: true
                type: string
              netboxId:
                description: NetBox IP address ID (set after creation)
                format: uint64
                minimum: 0.0
                nullable: true
                type: integer
              netboxUrl:
                description: NetBox IP address URL
                nullable: true
                type: string
              state:
                description: Current state of the IP address
                enum:
                - Pending
                - Created
                - Updated
                - Failed
                type: string
            required:
            - state
            type: object
        required:
        - spec
        title: NetBoxIPAddress
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: netboxipranges.dcops.microscaler.io
spec:
  group: dcops.microscaler.io
  names:
    categories: []
    kind: NetBoxIPRange
    plural: netboxipranges
    shortNames: []
    singular: netboxiprange
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for NetBoxIPRangeSpec via `CustomResource`
        properties:
          spec:
            description: NetBoxIPRangeSpec defines the desired state of a NetBox IP range
            properties:
              description:
                description: Description
                nullable: true
                type: string
              endAddress:
                description: |-
                  End IP address with CIDR (e.g., "192.168.1.200/24" or "2001:db8::200/64")
                  Must be a valid CIDR notation (IP address with prefix length)
                  Must be in the same family (IPv4 or IPv6) as start_address
                pattern: ^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.){3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)/(?:[0-9]|[12][0-9]|3[0-2])$|^(?:[0-9a-fA-F]{1,4}:){7}[0-9a-fA-F]{1,4}/(?:[0-9]|[1-9][0-9]|1[0-2][0-8])$
                type: string
              markPopulated:
                default: false
                description: |-
                  Mark as populated (for DHCP ranges, set to true)
                  When true, all IPs in the range are considered populated
                type: boolean
              markUtilized:
                default: false
                description: |-
                  Mark as utilized (for DHCP ranges, set to true)
                  When true, all IPs in the range are considered utilized
                type: boolean
              role:
                description: IP range role reference (references NetBoxRole CRD, optional)
                nullable: true
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
              startAddress:
                description: |-
                  Start IP address with CIDR (e.g., "192.168.1.100/24" or "2001:db8::100/64")
                  Must be a valid CIDR notation (IP address with prefix length)
                pattern: ^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.){3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)/(?:[0-9]|[12][0-9]|3[0-2])$|^(?:[0-9a-fA-F]{1,4}:){7}[0-9a-fA-F]{1,4}/(?:[0-9]|[1-9][0-9]|1[0-2][0-8])$
                type: string
              status:
                default: active
                description: IP range status (active, reserved, deprecated)
                enum:
                - active
                - reserved
                - deprecated
                type: string
              tags:
                description: Tag references (references NetBoxTag CRDs)
                items:
                  description: |-
                    Kubernetes-compliant resource reference for NetBox CRDs

                    This follows the Kubernetes `TypedLocalObjectReference` pattern, which includes:
                    - `apiGroup`: The API group of the referenced resource (e.g., "dcops.microscaler.io")
                    - `kind`: The kind of the referenced resource (e.g., "NetBoxSite")
                    - `name`: The name of the referenced resource (required)
                    - `namespace`: The namespace of the referenced resource (optional, defaults to same namespace)

                    This enables Kubernetes to validate reference types and provides clear documentation
                    of what resource type is expected.
                  properties:
                    apiGroup:
                      description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                      type: string
                    kind:
                      description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                      type: string
                    name:
                      description: Name of the referenced NetBox CRD resource
                      type: string
                    namespace:
                      description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                      nullable: true
                      type: string
                  required:
                  - apiGroup
                  - kind
                  - name
                  type: object
                nullable: true
                type: array
              tenant:
                description: |-
                  Tenant reference (references NetBoxTenant CRD, required)
                  Tenant is required in NetBox for proper resource organization and access control
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
              vrf:
                description: VRF reference (references NetBoxVRF CRD, optional - not yet implemented)
                nullable: true
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
            required:
            - endAddress
            - startAddress
            - tenant
            type: object
          status:
            description: NetBoxIPRangeStatus defines the observed state of a NetBox IP range
            nullable: true
            properties:
              error:
                description: Error message if reconciliation failed
                nullable: true
                type: string
              lastReconciled:
                description: Last reconciliation timestamp
                format: date-time
                nullable: true
                type: string
              netboxId:
                description: NetBox IP range ID (set after creation)
                format: uint64
                minimum: 0.0
                nullable: true
                type: integer
              netboxUrl:
                description: NetBox IP range URL
                nullable: true
                type: string
              state:
                description: Current state of the IP range
                enum:
                - Pending
                - Created
                - Updated
                - Failed
                type: string
            required:
            - state
            type: object
        required:
        - spec
        title: NetBoxIPRange
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: netboxroles.dcops.microscaler.io
spec:
  group: dcops.microscaler.io
  names:
    categories: []
    kind: NetBoxRole
    plural: netboxroles
    shortNames: []
    singular: netboxrole
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for NetBoxRoleSpec via `CustomResource`
        properties:
          spec:
            description: NetBoxRoleSpec defines the desired state of a NetBox role
            properties:
              comments:
                description: Comments
                nullable: true
                type: string
              description:
                description: Description of the role
                nullable: true
                type: string
              name:
                description: Role name
                type: string
              slug:
                description: Role slug (optional, auto-generated from name if not provided)
                nullable: true
                type: string
              tags:
                description: Tag references (references NetBoxTag CRDs)
                items:
                  description: |-
                    Kubernetes-compliant resource reference for NetBox CRDs

                    This follows the Kubernetes `TypedLocalObjectReference` pattern, which includes:
                    - `apiGroup`: The API group of the referenced resource (e.g., "dcops.microscaler.io")
                    - `kind`: The kind of the referenced resource (e.g., "NetBoxSite")
                    - `name`: The name of the referenced resource (required)
                    - `namespace`: The namespace of the referenced resource (optional, defaults to same namespace)

                    This enables Kubernetes to validate reference types and provides clear documentation
                    of what resource type is expected.
                  properties:
                    apiGroup:
                      description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                      type: string
                    kind:
                      description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                      type: string
                    name:
                      description: Name of the referenced NetBox CRD resource
                      type: string
                    namespace:
                      description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                      nullable: true
                      type: string
                  required:
                  - apiGroup
                  - kind
                  - name
                  type: object
                nullable: true
                type: array
              weight:
                description: Weight (for ordering)
                format: uint16
                maximum: 65535.0
                minimum: 0.0
                nullable: true
                type: integer
            required:
            - name
            type: object
          status:
            description: NetBoxRoleStatus defines the observed state of a NetBox role
            nullable: true
            properties:
              error:
                description: Error message if reconciliation failed
                nullable: true
                type: string
              lastReconciled:
                description: Last reconciliation timestamp
                format: date-time
                nullable: true
                type: string
              netboxId:
                description: NetBox role ID (set after creation)
                format: uint64
                minimum: 0.0
                nullable: true
                type: integer
              netboxUrl:
                description: NetBox role URL
                nullable: true
                type: string
              state:
                description: Current state of the role
                enum:
                - Pending
                - Created
                - Updated
                - Failed
                type: string
            required:
            - state
            type: object
        required:
        - spec
        title: NetBoxRole
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: netboxtags.dcops.microscaler.io
spec:
  group: dcops.microscaler.io
  names:
    categories: []
    kind: NetBoxTag
    plural: netboxtags
    shortNames: []
    singular: netboxtag
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for NetBoxTagSpec via `CustomResource`
        properties:
          spec:
            description: NetBoxTagSpec defines the desired state of a NetBox tag
            properties:
              color:
                description: Tag color (hex color code, e.g., "9e9e9e")
                nullable: true
                type: string
              comments:
                description: Comments
                nullable: true
                type: string
              description:
                description: Description of the tag
                nullable: true
                type: string
              name:
                description: Tag name
                type: string
              slug:
                description: Tag slug (optional, auto-generated from name if not provided)
                nullable: true
                type: string
              tenant:
                description: |-
                  Tenant reference (optional)

                  If specified, the tag will be managed using this tenant's NetBox API token.
                  If not specified, the controller will attempt to find a tenant from resources
                  that reference this tag, or fall back to the system tenant.

                  Note: Tags in NetBox are global resources and don't have a tenant field.
                  This field only determines which API token to use for tag operations.
                nullable: true
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
            required:
            - name
            type: object
          status:
            description: NetBoxTagStatus defines the observed state of a NetBox tag
            nullable: true
            properties:
              error:
                description: Error message if reconciliation failed
                nullable: true
                type: string
              lastReconciled:
                description: Last reconciliation timestamp
                format: date-time
                nullable: true
                type: string
              netboxId:
                description: NetBox tag ID (set after creation)
                format: uint64
                minimum: 0.0
                nullable: true
                type: integer
              netboxUrl:
                description: NetBox tag URL
                nullable: true
                type: string
              state:
                description: Current state of the tag
                enum:
                - Pending
                - Created
                - Updated
                - Failed
                type: string
            required:
            - state
            type: object
        required:
        - spec
        title: NetBoxTag
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: netboxaggregates.dcops.microscaler.io
spec:
  group: dcops.microscaler.io
  names:
    categories: []
    kind: NetBoxAggregate
    plural: netboxaggregates
    shortNames: []
    singular: netboxaggregate
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for NetBoxAggregateSpec via `CustomResource`
        properties:
          spec:
            description: NetBoxAggregateSpec defines the desired state of a NetBox aggregate
            properties:
              comments:
                description: Comments
                nullable: true
                type: string
              dateAllocated:
                description: Date allocated
                nullable: true
                type: string
              description:
                description: Description of the aggregate
                nullable: true
                type: string
              prefix:
                description: |-
                  Aggregate prefix (e.g., "192.168.0.0/16" or "2001:db8::/32")
                  Must be a valid CIDR notation (IP address with prefix length)
                pattern: ^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.){3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)/(?:[0-9]|[12][0-9]|3[0-2])$|^(?:[0-9a-fA-F]{1,4}:){7}[0-9a-fA-F]{1,4}/(?:[0-9]|[1-9][0-9]|1[0-2][0-8])$
                type: string
              rir:
                description: RIR (Regional Internet Registry) - ARIN, RIPE, APNIC, etc.
                nullable: true
                type: string
              tags:
                description: Tag references (references NetBoxTag CRDs)
                items:
                  description: |-
                    Kubernetes-compliant resource reference for NetBox CRDs

                    This follows the Kubernetes `TypedLocalObjectReference` pattern, which includes:
                    - `apiGroup`: The API group of the referenced resource (e.g., "dcops.microscaler.io")
                    - `kind`: The kind of the referenced resource (e.g., "NetBoxSite")
                    - `name`: The name of the referenced resource (required)
                    - `namespace`: The namespace of the referenced resource (optional, defaults to same namespace)

                    This enables Kubernetes to validate reference types and provides clear documentation
                    of what resource type is expected.
                  properties:
                    apiGroup:
                      description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                      type: string
                    kind:
                      description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                      type: string
                    name:
                      description: Name of the referenced NetBox CRD resource
                      type: string
                    namespace:
                      description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                      nullable: true
                      type: string
                  required:
                  - apiGroup
                  - kind
                  - name
                  type: object
                nullable: true
                type: array
            required:
            - prefix
            type: object
          status:
            description: NetBoxAggregateStatus defines the observed state of a NetBox aggregate
            nullable: true
            properties:
              error:
                description: Error message if reconciliation failed
                nullable: true
                type: string
              lastReconciled:
                description: Last reconciliation timestamp
                format: date-time
                nullable: true
                type: string
              netboxId:
                description: NetBox aggregate ID (set after creation)
                format: uint64
                minimum: 0.0
                nullable: true
                type: integer
              netboxUrl:
                description: NetBox aggregate URL
                nullable: true
                type: string
              state:
                description: Current state of the aggregate
                enum:
                - Pending
                - Created
                - Updated
                - Failed
                type: string
            required:
            - state
            type: object
        required:
        - spec
        title: NetBoxAggregate
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: netboxvlans.dcops.microscaler.io
spec:
  group: dcops.microscaler.io
  names:
    categories: []
    kind: NetBoxVLAN
    plural: netboxvlans
    shortNames: []
    singular: netboxvlan
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for NetBoxVLANSpec via `CustomResource`
        properties:
          spec:
            description: NetBoxVLANSpec defines the desired state of a NetBox VLAN
            properties:
              comments:
                description: Comments
                nullable: true
                type: string
              description:
                description: Description
                nullable: true
                type: string
              group:
                description: VLAN group reference (references NetBoxVLANGroup CRD, optional - not yet implemented)
                nullable: true
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
              name:
                description: VLAN name
                type: string
              role:
                description: Role reference (references NetBoxRole CRD, optional)
                nullable: true
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
              site:
                description: Site reference (references NetBoxSite CRD, optional)
                nullable: true
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
              status:
                default: active
                description: VLAN status (active, reserved, deprecated)
                enum:
                - active
                - reserved
                - deprecated
                type: string
              tags:
                description: Tag references (references NetBoxTag CRDs)
                items:
                  description: |-
                    Kubernetes-compliant resource reference for NetBox CRDs

                    This follows the Kubernetes `TypedLocalObjectReference` pattern, which includes:
                    - `apiGroup`: The API group of the referenced resource (e.g., "dcops.microscaler.io")
                    - `kind`: The kind of the referenced resource (e.g., "NetBoxSite")
                    - `name`: The name of the referenced resource (required)
                    - `namespace`: The namespace of the referenced resource (optional, defaults to same namespace)

                    This enables Kubernetes to validate reference types and provides clear documentation
                    of what resource type is expected.
                  properties:
                    apiGroup:
                      description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                      type: string
                    kind:
                      description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                      type: string
                    name:
                      description: Name of the referenced NetBox CRD resource
                      type: string
                    namespace:
                      description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                      nullable: true
                      type: string
                  required:
                  - apiGroup
                  - kind
                  - name
                  type: object
                nullable: true
                type: array
              tenant:
                description: |-
                  Tenant reference (references NetBoxTenant CRD, required)
                  Tenant is required in NetBox for proper resource organization and access control
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
              vid:
                description: VLAN ID (1-4094)
                format: uint16
                maximum: 65535.0
                minimum: 0.0
                type: integer
            required:
            - name
            - tenant
            - vid
            type: object
          status:
            description: NetBoxVLANStatus defines the observed state of a NetBox VLAN
            nullable: true
            properties:
              error:
                description: Error message if reconciliation failed
                nullable: true
                type: string
              lastReconciled:
                description: Last reconciliation timestamp
                format: date-time
                nullable: true
                type: string
              netboxId:
                description: NetBox VLAN ID (set after creation)
                format: uint64
                minimum: 0.0
                nullable: true
                type: integer
              netboxUrl:
                description: NetBox VLAN URL
                nullable: true
                type: string
              state:
                description: Current state of the VLAN
                enum:
                - Pending
                - Created
                - Updated
                - Failed
                type: string
            required:
            - state
            type: object
        required:
        - spec
        title: NetBoxVLAN
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: netboxrirs.dcops.microscaler.io
spec:
  group: dcops.microscaler.io
  names:
    categories: []
    kind: NetBoxRIR
    plural: netboxrirs
    shortNames: []
    singular: netboxrir
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for NetBoxRIRSpec via `CustomResource`
        properties:
          spec:
            description: NetBoxRIRSpec defines the desired state of a NetBox RIR
            properties:
              description:
                description: Description of the RIR
                nullable: true
                type: string
              isPrivate:
                description: 'Whether this is a private RIR (default: false)'
                nullable: true
                type: boolean
              name:
                description: RIR name (e.g., "ARIN", "RIPE", "APNIC")
                type: string
              slug:
                description: RIR slug (optional, auto-generated from name if not provided)
                nullable: true
                type: string
              tags:
                description: Tag references (references NetBoxTag CRDs)
                items:
                  description: |-
                    Kubernetes-compliant resource reference for NetBox CRDs

                    This follows the Kubernetes `TypedLocalObjectReference` pattern, which includes:
                    - `apiGroup`: The API group of the referenced resource (e.g., "dcops.microscaler.io")
                    - `kind`: The kind of the referenced resource (e.g., "NetBoxSite")
                    - `name`: The name of the referenced resource (required)
                    - `namespace`: The namespace of the referenced resource (optional, defaults to same namespace)

                    This enables Kubernetes to validate reference types and provides clear documentation
                    of what resource type is expected.
                  properties:
                    apiGroup:
                      description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                      type: string
                    kind:
                      description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                      type: string
                    name:
                      description: Name of the referenced NetBox CRD resource
                      type: string
                    namespace:
                      description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                      nullable: true
                      type: string
                  required:
                  - apiGroup
                  - kind
                  - name
                  type: object
                nullable: true
                type: array
            required:
            - name
            type: object
          status:
            description: NetBoxRIRStatus defines the observed state of a NetBox RIR
            nullable: true
            properties:
              error:
                description: Error message if reconciliation failed
                nullable: true
                type: string
              lastReconciled:
                description: Last reconciliation timestamp
                format: date-time
                nullable: true
                type: string
              netboxId:
                description: NetBox RIR ID (set after creation)
                format: uint64
                minimum: 0.0
                nullable: true
                type: integer
              netboxUrl:
                description: NetBox RIR URL
                nullable: true
                type: string
              state:
                description: Current state of the RIR
                enum:
                - Pending
                - Created
                - Updated
                - Failed
                type: string
            required:
            - state
            type: object
        required:
        - spec
        title: NetBoxRIR
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: netboxvrfs.dcops.microscaler.io
spec:
  group: dcops.microscaler.io
  names:
    categories: []
    kind: NetBoxVRF
    plural: netboxvrfs
    shortNames: []
    singular: netboxvrf
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for NetBoxVRFSpec via `CustomResource`
        properties:
          spec:
            description: NetBoxVRFSpec defines the desired state of a NetBox VRF
            properties:
              comments:
                description: |-
                  Comments (optional)
                  Additional notes or documentation about this VRF
                nullable: true
                type: string
              description:
                description: |-
                  Description (optional)
                  Human-readable description of the VRF's purpose
                nullable: true
                type: string
              enforceUnique:
                default: false
                description: |-
                  Enforce unique IP space (optional, default: false)
                  When true, prevents duplicate prefixes/IPs within this VRF
                  Useful for ensuring IP address uniqueness across overlapping address spaces
                type: boolean
              exportTargets:
                description: |-
                  Export Route Targets (optional)
                  List of Route Target references for exporting routes from this VRF
                  Routes from this VRF will be tagged with these route targets for export
                items:
                  description: |-
                    Kubernetes-compliant resource reference for NetBox CRDs

                    This follows the Kubernetes `TypedLocalObjectReference` pattern, which includes:
                    - `apiGroup`: The API group of the referenced resource (e.g., "dcops.microscaler.io")
                    - `kind`: The kind of the referenced resource (e.g., "NetBoxSite")
                    - `name`: The name of the referenced resource (required)
                    - `namespace`: The namespace of the referenced resource (optional, defaults to same namespace)

                    This enables Kubernetes to validate reference types and provides clear documentation
                    of what resource type is expected.
                  properties:
                    apiGroup:
                      description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                      type: string
                    kind:
                      description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                      type: string
                    name:
                      description: Name of the referenced NetBox CRD resource
                      type: string
                    namespace:
                      description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                      nullable: true
                      type: string
                  required:
                  - apiGroup
                  - kind
                  - name
                  type: object
                nullable: true
                type: array
              importTargets:
                description: |-
                  Import Route Targets (optional)
                  List of Route Target references for importing routes into this VRF
                  Routes with these route targets will be imported into this VRF
                items:
                  description: |-
                    Kubernetes-compliant resource reference for NetBox CRDs

                    This follows the Kubernetes `TypedLocalObjectReference` pattern, which includes:
                    - `apiGroup`: The API group of the referenced resource (e.g., "dcops.microscaler.io")
                    - `kind`: The kind of the referenced resource (e.g., "NetBoxSite")
                    - `name`: The name of the referenced resource (required)
                    - `namespace`: The namespace of the referenced resource (optional, defaults to same namespace)

                    This enables Kubernetes to validate reference types and provides clear documentation
                    of what resource type is expected.
                  properties:
                    apiGroup:
                      description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                      type: string
                    kind:
                      description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                      type: string
                    name:
                      description: Name of the referenced NetBox CRD resource
                      type: string
                    namespace:
                      description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                      nullable: true
                      type: string
                  required:
                  - apiGroup
                  - kind
                  - name
                  type: object
                nullable: true
                type: array
              name:
                description: |-
                  VRF name (required)
                  Administrative name for the VRF
                type: string
              rd:
                description: |-
                  Route Distinguisher (optional)
                  RFC 4364 format (e.g., "65000:1" or "192.168.1.1:1")
                  Used to distinguish routes within the VRF
                  If not specified, NetBox will auto-generate one
                nullable: true
                type: string
              tags:
                description: Tag references (references NetBoxTag CRDs)
                items:
                  description: |-
                    Kubernetes-compliant resource reference for NetBox CRDs

                    This follows the Kubernetes `TypedLocalObjectReference` pattern, which includes:
                    - `apiGroup`: The API group of the referenced resource (e.g., "dcops.microscaler.io")
                    - `kind`: The kind of the referenced resource (e.g., "NetBoxSite")
                    - `name`: The name of the referenced resource (required)
                    - `namespace`: The namespace of the referenced resource (optional, defaults to same namespace)

                    This enables Kubernetes to validate reference types and provides clear documentation
                    of what resource type is expected.
                  properties:
                    apiGroup:
                      description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                      type: string
                    kind:
                      description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                      type: string
                    name:
                      description: Name of the referenced NetBox CRD resource
                      type: string
                    namespace:
                      description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                      nullable: true
                      type: string
                  required:
                  - apiGroup
                  - kind
                  - name
                  type: object
                nullable: true
                type: array
              tenant:
                description: |-
                  Tenant reference (optional)
                  VRFs can be scoped to a tenant for multi-tenancy
                nullable: true
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
            required:
            - name
            type: object
          status:
            description: NetBoxVRFStatus defines the observed state of a NetBox VRF
            nullable: true
            properties:
              error:
                description: Error message if reconciliation failed
                nullable: true
                type: string
              lastReconciled:
                description: Last reconciliation timestamp
                format: date-time
                nullable: true
                type: string
              netboxId:
                description: NetBox VRF ID (set after creation)
                format: uint64
                minimum: 0.0
                nullable: true
                type: integer
              netboxUrl:
                description: NetBox VRF URL
                nullable: true
                type: string
              state:
                description: Current state of the VRF
                enum:
                - Pending
                - Created
                - Updated
                - Failed
                type: string
            required:
            - state
            type: object
        required:
        - spec
        title: NetBoxVRF
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: netboxroutetargets.dcops.microscaler.io
spec:
  group: dcops.microscaler.io
  names:
    categories: []
    kind: NetBoxRouteTarget
    plural: netboxroutetargets
    shortNames: []
    singular: netboxroutetarget
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for NetBoxRouteTargetSpec via `CustomResource`
        properties:
          spec:
            description: NetBoxRouteTargetSpec defines the desired state of a NetBox Route Target
            properties:
              comments:
                description: |-
                  Comments (optional)
                  Additional notes or documentation about this route target
                nullable: true
                type: string
              description:
                description: |-
                  Description (optional)
                  Human-readable description of the route target's purpose
                nullable: true
                type: string
              name:
                description: |-
                  Route Target name (required)
                  Format: RFC 4360 format (e.g., "65000:100" or "65000:100:200")
                  This is a unique identifier for the route target
                type: string
              tags:
                description: Tag references (references NetBoxTag CRDs)
                items:
                  description: |-
                    Kubernetes-compliant resource reference for NetBox CRDs

                    This follows the Kubernetes `TypedLocalObjectReference` pattern, which includes:
                    - `apiGroup`: The API group of the referenced resource (e.g., "dcops.microscaler.io")
                    - `kind`: The kind of the referenced resource (e.g., "NetBoxSite")
                    - `name`: The name of the referenced resource (required)
                    - `namespace`: The namespace of the referenced resource (optional, defaults to same namespace)

                    This enables Kubernetes to validate reference types and provides clear documentation
                    of what resource type is expected.
                  properties:
                    apiGroup:
                      description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                      type: string
                    kind:
                      description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                      type: string
                    name:
                      description: Name of the referenced NetBox CRD resource
                      type: string
                    namespace:
                      description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                      nullable: true
                      type: string
                  required:
                  - apiGroup
                  - kind
                  - name
                  type: object
                nullable: true
                type: array
              tenant:
                description: |-
                  Tenant reference (optional)
                  Route targets can be scoped to a tenant for multi-tenancy
                nullable: true
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
            required:
            - name
            type: object
          status:
            description: NetBoxRouteTargetStatus defines the observed state of a NetBox Route Target
            nullable: true
            properties:
              error:
                description: Error message if reconciliation failed
                nullable: true
                type: string
              lastReconciled:
                description: Last reconciliation timestamp
                format: date-time
                nullable: true
                type: string
              netboxId:
                description: NetBox Route Target ID (set after creation)
                format: uint64
                minimum: 0.0
                nullable: true
                type: integer
              netboxUrl:
                description: NetBox Route Target URL
                nullable: true
                type: string
              state:
                description: Current state of the Route Target
                enum:
                - Pending
                - Created
                - Updated
                - Failed
                type: string
            required:
            - state
            type: object
        required:
        - spec
        title: NetBoxRouteTarget
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: bootprofiles.dcops.microscaler.io
spec:
  group: dcops.microscaler.io
  names:
    categories: []
    kind: BootProfile
    plural: bootprofiles
    shortNames: []
    singular: bootprofile
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for BootProfileSpec via `CustomResource`
        properties:
          spec:
            properties:
              cmdline:
                default: ''
                description: Kernel command-line parameters
                type: string
              initrd:
                default: []
                description: Initrd image URLs or paths
                items:
                  type: string
                type: array
              kernel:
                description: Kernel image URL or path
                type: string
              message:
                description: Boot message to display (optional)
                nullable: true
                type: string
              schematicId:
                description: Talos Image Factory schematic ID (for Raspberry Pi custom images)
                nullable: true
                type: string
            required:
            - kernel
            type: object
          status:
            nullable: true
            properties:
              lastReconciled:
                description: Last reconciliation timestamp
                format: date-time
                nullable: true
                type: string
              ready:
                description: Whether the boot profile is ready
                type: boolean
            required:
            - ready
            type: object
        required:
        - spec
        title: BootProfile
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: bootintents.dcops.microscaler.io
spec:
  group: dcops.microscaler.io
  names:
    categories: []
    kind: BootIntent
    plural: bootintents
    shortNames: []
    singular: bootintent
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for BootIntentSpec via `CustomResource`
        properties:
          spec:
            properties:
              lifecycle:
                default: discovered
                description: Lifecycle state
                enum:
                - discovered
                - installing
                - installed
                - locked
                type: string
              macAddress:
                description: MAC address of the machine
                type: string
              profileRef:
                description: Reference to BootProfile
                properties:
                  name:
                    description: Name of the BootProfile
                    type: string
                  namespace:
                    description: Namespace (defaults to same namespace as BootIntent)
                    nullable: true
                    type: string
                required:
                - name
                type: object
            required:
            - macAddress
            - profileRef
            type: object
          status:
            nullable: true
            properties:
              configured:
                description: Whether boot intent is configured
                type: boolean
              error:
                description: Error message if reconciliation failed
                nullable: true
                type: string
              lastReconciled:
                description: Last reconciliation timestamp
                format: date-time
                nullable: true
                type: string
              lifecycle:
                description: Current lifecycle state
                enum:
                - discovered
                - installing
                - installed
                - locked
                type: string
            required:
            - configured
            - lifecycle
            type: object
        required:
        - spec
        title: BootIntent
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: netboxdeviceroles.dcops.microscaler.io
spec:
  group: dcops.microscaler.io
  names:
    categories: []
    kind: NetBoxDeviceRole
    plural: netboxdeviceroles
    shortNames: []
    singular: netboxdevicerole
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for NetBoxDeviceRoleSpec via `CustomResource`
        properties:
          spec:
            description: NetBoxDeviceRoleSpec defines the desired state of a NetBox device role
            properties:
              color:
                description: Color (hex code, e.g., "9e9e9e")
                nullable: true
                type: string
              comments:
                description: Comments
                nullable: true
                type: string
              description:
                description: Description of the device role
                nullable: true
                type: string
              name:
                description: Device role name
                type: string
              slug:
                description: Device role slug (optional, auto-generated from name if not provided)
                nullable: true
                type: string
              tags:
                description: Tag references (references NetBoxTag CRDs)
                items:
                  description: |-
                    Kubernetes-compliant resource reference for NetBox CRDs

                    This follows the Kubernetes `TypedLocalObjectReference` pattern, which includes:
                    - `apiGroup`: The API group of the referenced resource (e.g., "dcops.microscaler.io")
                    - `kind`: The kind of the referenced resource (e.g., "NetBoxSite")
                    - `name`: The name of the referenced resource (required)
                    - `namespace`: The namespace of the referenced resource (optional, defaults to same namespace)

                    This enables Kubernetes to validate reference types and provides clear documentation
                    of what resource type is expected.
                  properties:
                    apiGroup:
                      description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                      type: string
                    kind:
                      description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                      type: string
                    name:
                      description: Name of the referenced NetBox CRD resource
                      type: string
                    namespace:
                      description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                      nullable: true
                      type: string
                  required:
                  - apiGroup
                  - kind
                  - name
                  type: object
                nullable: true
                type: array
              vmRole:
                default: false
                description: Virtual machine role (if true, can be assigned to VMs)
                type: boolean
            required:
            - name
            type: object
          status:
            description: NetBoxDeviceRoleStatus defines the observed state of a NetBox device role
            nullable: true
            properties:
              error:
                description: Error message if reconciliation failed
                nullable: true
                type: string
              lastReconciled:
                description: Last reconciliation timestamp
                format: date-time
                nullable: true
                type: string
              netboxId:
                description: NetBox device role ID (set after creation)
                format: uint64
                minimum: 0.0
                nullable: true
                type: integer
              netboxUrl:
                description: NetBox device role URL
                nullable: true
                type: string
              state:
                description: Current state of the device role
                enum:
                - Pending
                - Created
                - Updated
                - Failed
                type: string
            required:
            - state
            type: object
        required:
        - spec
        title: NetBoxDeviceRole
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: netboxmanufacturers.dcops.microscaler.io
spec:
  group: dcops.microscaler.io
  names:
    categories: []
    kind: NetBoxManufacturer
    plural: netboxmanufacturers
    shortNames: []
    singular: netboxmanufacturer
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for NetBoxManufacturerSpec via `CustomResource`
        properties:
          spec:
            description: NetBoxManufacturerSpec defines the desired state of a NetBox manufacturer
            properties:
              description:
                description: Description of the manufacturer
                nullable: true
                type: string
              name:
                description: Manufacturer name
                type: string
              slug:
                description: Manufacturer slug (optional, auto-generated from name if not provided)
                nullable: true
                type: string
              tags:
                description: Tag references (references NetBoxTag CRDs)
                items:
                  description: |-
                    Kubernetes-compliant resource reference for NetBox CRDs

                    This follows the Kubernetes `TypedLocalObjectReference` pattern, which includes:
                    - `apiGroup`: The API group of the referenced resource (e.g., "dcops.microscaler.io")
                    - `kind`: The kind of the referenced resource (e.g., "NetBoxSite")
                    - `name`: The name of the referenced resource (required)
                    - `namespace`: The namespace of the referenced resource (optional, defaults to same namespace)

                    This enables Kubernetes to validate reference types and provides clear documentation
                    of what resource type is expected.
                  properties:
                    apiGroup:
                      description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                      type: string
                    kind:
                      description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                      type: string
                    name:
                      description: Name of the referenced NetBox CRD resource
                      type: string
                    namespace:
                      description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                      nullable: true
                      type: string
                  required:
                  - apiGroup
                  - kind
                  - name
                  type: object
                nullable: true
                type: array
            required:
            - name
            type: object
          status:
            description: NetBoxManufacturerStatus defines the observed state of a NetBox manufacturer
            nullable: true
            properties:
              error:
                description: Error message if reconciliation failed
                nullable: true
                type: string
              lastReconciled:
                description: Last reconciliation timestamp
                format: date-time
                nullable: true
                type: string
              netboxId:
                description: NetBox manufacturer ID (set after creation)
                format: uint64
                minimum: 0.0
                nullable: true
                type: integer
              netboxUrl:
                description: NetBox manufacturer URL
                nullable: true
                type: string
              state:
                description: Current state of the manufacturer
                enum:
                - Pending
                - Created
                - Updated
                - Failed
                type: string
            required:
            - state
            type: object
        required:
        - spec
        title: NetBoxManufacturer
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: netboxplatforms.dcops.microscaler.io
spec:
  group: dcops.microscaler.io
  names:
    categories: []
    kind: NetBoxPlatform
    plural: netboxplatforms
    shortNames: []
    singular: netboxplatform
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for NetBoxPlatformSpec via `CustomResource`
        properties:
          spec:
            description: NetBoxPlatformSpec defines the desired state of a NetBox platform
            properties:
              comments:
                description: Comments
                nullable: true
                type: string
              description:
                description: Description of the platform
                nullable: true
                type: string
              manufacturer:
                description: Manufacturer reference (references NetBoxManufacturer CRD)
                nullable: true
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
              name:
                description: Platform name (e.g., "Talos Linux", "Ubuntu")
                type: string
              napalmArgs:
                description: NAPALM arguments (JSON string)
                nullable: true
                type: string
              napalmDriver:
                description: NAPALM driver (for network automation)
                nullable: true
                type: string
              slug:
                description: Platform slug (optional, auto-generated from name if not provided)
                nullable: true
                type: string
              tags:
                description: Tag references (references NetBoxTag CRDs)
                items:
                  description: |-
                    Kubernetes-compliant resource reference for NetBox CRDs

                    This follows the Kubernetes `TypedLocalObjectReference` pattern, which includes:
                    - `apiGroup`: The API group of the referenced resource (e.g., "dcops.microscaler.io")
                    - `kind`: The kind of the referenced resource (e.g., "NetBoxSite")
                    - `name`: The name of the referenced resource (required)
                    - `namespace`: The namespace of the referenced resource (optional, defaults to same namespace)

                    This enables Kubernetes to validate reference types and provides clear documentation
                    of what resource type is expected.
                  properties:
                    apiGroup:
                      description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                      type: string
                    kind:
                      description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                      type: string
                    name:
                      description: Name of the referenced NetBox CRD resource
                      type: string
                    namespace:
                      description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                      nullable: true
                      type: string
                  required:
                  - apiGroup
                  - kind
                  - name
                  type: object
                nullable: true
                type: array
            required:
            - name
            type: object
          status:
            description: NetBoxPlatformStatus defines the observed state of a NetBox platform
            nullable: true
            properties:
              error:
                description: Error message if reconciliation failed
                nullable: true
                type: string
              lastReconciled:
                description: Last reconciliation timestamp
                format: date-time
                nullable: true
                type: string
              netboxId:
                description: NetBox platform ID (set after creation)
                format: uint64
                minimum: 0.0
                nullable: true
                type: integer
              netboxUrl:
                description: NetBox platform URL
                nullable: true
                type: string
              state:
                description: Current state of the platform
                enum:
                - Pending
                - Created
                - Updated
                - Failed
                type: string
            required:
            - state
            type: object
        required:
        - spec
        title: NetBoxPlatform
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: netboxdevicetypes.dcops.microscaler.io
spec:
  group: dcops.microscaler.io
  names:
    categories: []
    kind: NetBoxDeviceType
    plural: netboxdevicetypes
    shortNames: []
    singular: netboxdevicetype
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for NetBoxDeviceTypeSpec via `CustomResource`
        properties:
          spec:
            description: NetBoxDeviceTypeSpec defines the desired state of a NetBox device type
            properties:
              comments:
                description: Comments
                nullable: true
                type: string
              description:
                description: Description of the device type
                nullable: true
                type: string
              isFullDepth:
                default: false
                description: 'Is full depth (default: false)'
                type: boolean
              manufacturer:
                description: Manufacturer reference (references NetBoxManufacturer CRD)
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
              model:
                description: Device model (e.g., "Raspberry Pi 4 Model B")
                type: string
              partNumber:
                description: Part number
                nullable: true
                type: string
              slug:
                description: Device type slug (optional, auto-generated from model if not provided)
                nullable: true
                type: string
              tags:
                description: Tag references (references NetBoxTag CRDs)
                items:
                  description: |-
                    Kubernetes-compliant resource reference for NetBox CRDs

                    This follows the Kubernetes `TypedLocalObjectReference` pattern, which includes:
                    - `apiGroup`: The API group of the referenced resource (e.g., "dcops.microscaler.io")
                    - `kind`: The kind of the referenced resource (e.g., "NetBoxSite")
                    - `name`: The name of the referenced resource (required)
                    - `namespace`: The namespace of the referenced resource (optional, defaults to same namespace)

                    This enables Kubernetes to validate reference types and provides clear documentation
                    of what resource type is expected.
                  properties:
                    apiGroup:
                      description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                      type: string
                    kind:
                      description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                      type: string
                    name:
                      description: Name of the referenced NetBox CRD resource
                      type: string
                    namespace:
                      description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                      nullable: true
                      type: string
                  required:
                  - apiGroup
                  - kind
                  - name
                  type: object
                nullable: true
                type: array
              uHeight:
                default: 1.0
                description: 'U height (rack units, default: 1.0)'
                format: double
                type: number
            required:
            - manufacturer
            - model
            type: object
          status:
            description: NetBoxDeviceTypeStatus defines the observed state of a NetBox device type
            nullable: true
            properties:
              error:
                description: Error message if reconciliation failed
                nullable: true
                type: string
              lastReconciled:
                description: Last reconciliation timestamp
                format: date-time
                nullable: true
                type: string
              netboxId:
                description: NetBox device type ID (set after creation)
                format: uint64
                minimum: 0.0
                nullable: true
                type: integer
              netboxUrl:
                description: NetBox device type URL
                nullable: true
                type: string
              state:
                description: Current state of the device type
                enum:
                - Pending
                - Created
                - Updated
                - Failed
                type: string
            required:
            - state
            type: object
        required:
        - spec
        title: NetBoxDeviceType
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: netboxdevices.dcops.microscaler.io
spec:
  group: dcops.microscaler.io
  names:
    categories: []
    kind: NetBoxDevice
    plural: netboxdevices
    shortNames: []
    singular: netboxdevice
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for NetBoxDeviceSpec via `CustomResource`
        properties:
          spec:
            description: NetBoxDeviceSpec defines the desired state of a NetBox device
            properties:
              assetTag:
                description: Asset tag
                nullable: true
                type: string
              comments:
                description: Comments
                nullable: true
                type: string
              description:
                description: Description
                nullable: true
                type: string
              deviceRole:
                description: Device role reference (references NetBoxDeviceRole CRD)
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
              deviceType:
                description: Device type reference (references NetBoxDeviceType CRD)
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
              location:
                description: Location reference (references NetBoxLocation CRD, optional)
                nullable: true
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
              name:
                description: Device name (optional, but recommended)
                nullable: true
                type: string
              platform:
                description: Platform reference (references NetBoxPlatform CRD, optional)
                nullable: true
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
              primaryIp4:
                description: |-
                  Primary IPv4 address reference (optional)
                  Can be either:
                  - IPClaim CRD reference (recommended, GitOps-friendly) - use {"ipClaimRef": {...}}
                  - IP address string (e.g., "192.168.1.10/24") as fallback - use {"ipAddress": "..."}
                  Only one field should be set (ipClaimRef or ipAddress)
                nullable: true
                properties:
                  ipAddress:
                    description: |-
                      Direct IP address string (e.g., "192.168.1.10/24" or "2001:db8::1/64")
                      Used as fallback when IPClaim CRD is not available
                      Set this when providing a direct IP address
                      Must be a valid CIDR notation (IP address with prefix length)
                    nullable: true
                    pattern: ^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.){3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)/(?:[0-9]|[12][0-9]|3[0-2])$|^(?:[0-9a-fA-F]{1,4}:){7}[0-9a-fA-F]{1,4}/(?:[0-9]|[1-9][0-9]|1[0-2][0-8])$
                    type: string
                  ipClaimRef:
                    description: |-
                      IPClaim CRD reference (recommended, GitOps-friendly)
                      Set this when referencing an IPClaim CRD
                    nullable: true
                    properties:
                      apiGroup:
                        description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                        type: string
                      kind:
                        description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                        type: string
                      name:
                        description: Name of the referenced NetBox CRD resource
                        type: string
                      namespace:
                        description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                        nullable: true
                        type: string
                    required:
                    - apiGroup
                    - kind
                    - name
                    type: object
                type: object
              primaryIp6:
                description: |-
                  Primary IPv6 address reference (optional)
                  Can be either:
                  - IPClaim CRD reference (recommended, GitOps-friendly) - use {"ipClaimRef": {...}}
                  - IP address string (e.g., "2001:db8::1/64") as fallback - use {"ipAddress": "..."}
                  Only one field should be set (ipClaimRef or ipAddress)
                nullable: true
                properties:
                  ipAddress:
                    description: |-
                      Direct IP address string (e.g., "192.168.1.10/24" or "2001:db8::1/64")
                      Used as fallback when IPClaim CRD is not available
                      Set this when providing a direct IP address
                      Must be a valid CIDR notation (IP address with prefix length)
                    nullable: true
                    pattern: ^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.){3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)/(?:[0-9]|[12][0-9]|3[0-2])$|^(?:[0-9a-fA-F]{1,4}:){7}[0-9a-fA-F]{1,4}/(?:[0-9]|[1-9][0-9]|1[0-2][0-8])$
                    type: string
                  ipClaimRef:
                    description: |-
                      IPClaim CRD reference (recommended, GitOps-friendly)
                      Set this when referencing an IPClaim CRD
                    nullable: true
                    properties:
                      apiGroup:
                        description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                        type: string
                      kind:
                        description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                        type: string
                      name:
                        description: Name of the referenced NetBox CRD resource
                        type: string
                      namespace:
                        description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                        nullable: true
                        type: string
                    required:
                    - apiGroup
                    - kind
                    - name
                    type: object
                type: object
              serial:
                description: Serial number
                nullable: true
                type: string
              site:
                description: Site reference (references NetBoxSite CRD)
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
              status:
                default: active
                description: Device status (active, offline, planned, staged, failed, inventory)
                enum:
                - active
                - offline
                - planned
                - staged
                - failed
                - inventory
                - decommissioning
                type: string
              tags:
                description: Tag references (references NetBoxTag CRDs)
                items:
                  description: |-
                    Kubernetes-compliant resource reference for NetBox CRDs

                    This follows the Kubernetes `TypedLocalObjectReference` pattern, which includes:
                    - `apiGroup`: The API group of the referenced resource (e.g., "dcops.microscaler.io")
                    - `kind`: The kind of the referenced resource (e.g., "NetBoxSite")
                    - `name`: The name of the referenced resource (required)
                    - `namespace`: The namespace of the referenced resource (optional, defaults to same namespace)

                    This enables Kubernetes to validate reference types and provides clear documentation
                    of what resource type is expected.
                  properties:
                    apiGroup:
                      description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                      type: string
                    kind:
                      description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                      type: string
                    name:
                      description: Name of the referenced NetBox CRD resource
                      type: string
                    namespace:
                      description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                      nullable: true
                      type: string
                  required:
                  - apiGroup
                  - kind
                  - name
                  type: object
                nullable: true
                type: array
              tenant:
                description: |-
                  Tenant reference (references NetBoxTenant CRD, required)
                  Tenant is required in NetBox for proper resource organization and access control
                properties:
                  apiGroup:
                    description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                    type: string
                  kind:
                    description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                    type: string
                  name:
                    description: Name of the referenced NetBox CRD resource
                    type: string
                  namespace:
                    description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                    nullable: true
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
            required:
            - deviceRole
            - deviceType
            - site
            - tenant
            type: object
          status:
            description: NetBoxDeviceStatus defines the observed state of a NetBox device
            nullable: true
            properties:
              error:
                description: Error message if reconciliation failed
                nullable: true
                type: string
              lastReconciled:
                description: Last reconciliation timestamp
                format: date-time
                nullable: true
                type: string
              netboxId:
                description: NetBox device ID (set after creation)
                format: uint64
                minimum: 0.0
                nullable: true
                type: integer
              netboxUrl:
                description: NetBox device URL
                nullable: true
                type: string
              state:
                description: Current state of the device
                enum:
                - Pending
                - Created
                - Updated
                - Failed
                type: string
            required:
            - state
            type: object
        required:
        - spec
        title: NetBoxDevice
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: netboxinterfaces.dcops.microscaler.io
spec:
  group: dcops.microscaler.io
  names:
    categories: []
    kind: NetBoxInterface
    plural: netboxinterfaces
    shortNames: []
    singular: netboxinterface
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for NetBoxInterfaceSpec via `CustomResource`
        properties:
          spec:
            description: NetBoxInterfaceSpec defines the desired state of a NetBox interface
            properties:
              description:
                description: Description
                nullable: true
                type: string
              device:
                description: Device name (references NetBoxDevice CRD)
                type: string
              enabled:
                default: true
                description: 'Enabled (default: true)'
                type: boolean
              macAddress:
                description: MAC address (optional, can be managed via NetBoxMACAddress CRD)
                nullable: true
                type: string
              mtu:
                description: MTU (optional)
                format: uint16
                maximum: 65535.0
                minimum: 0.0
                nullable: true
                type: integer
              name:
                description: Interface name (e.g., "eth0", "wlan0")
                type: string
              tags:
                description: Tag references (references NetBoxTag CRDs)
                items:
                  description: |-
                    Kubernetes-compliant resource reference for NetBox CRDs

                    This follows the Kubernetes `TypedLocalObjectReference` pattern, which includes:
                    - `apiGroup`: The API group of the referenced resource (e.g., "dcops.microscaler.io")
                    - `kind`: The kind of the referenced resource (e.g., "NetBoxSite")
                    - `name`: The name of the referenced resource (required)
                    - `namespace`: The namespace of the referenced resource (optional, defaults to same namespace)

                    This enables Kubernetes to validate reference types and provides clear documentation
                    of what resource type is expected.
                  properties:
                    apiGroup:
                      description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                      type: string
                    kind:
                      description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                      type: string
                    name:
                      description: Name of the referenced NetBox CRD resource
                      type: string
                    namespace:
                      description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                      nullable: true
                      type: string
                  required:
                  - apiGroup
                  - kind
                  - name
                  type: object
                nullable: true
                type: array
              type:
                default: other
                description: Interface type (e.g., "1000base-t", "virtual", "other")
                type: string
            required:
            - device
            - name
            type: object
          status:
            description: NetBoxInterfaceStatus defines the observed state of a NetBox interface
            nullable: true
            properties:
              error:
                description: Error message if reconciliation failed
                nullable: true
                type: string
              lastReconciled:
                description: Last reconciliation timestamp
                format: date-time
                nullable: true
                type: string
              netboxId:
                description: NetBox interface ID (set after creation)
                format: uint64
                minimum: 0.0
                nullable: true
                type: integer
              netboxUrl:
                description: NetBox interface URL
                nullable: true
                type: string
              state:
                description: Current state of the interface
                enum:
                - Pending
                - Created
                - Updated
                - Failed
                type: string
            required:
            - state
            type: object
        required:
        - spec
        title: NetBoxInterface
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: netboxmacaddresses.dcops.microscaler.io
spec:
  group: dcops.microscaler.io
  names:
    categories: []
    kind: NetBoxMACAddress
    plural: netboxmacaddresses
    shortNames: []
    singular: netboxmacaddress
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for NetBoxMACAddressSpec via `CustomResource`
        properties:
          spec:
            description: NetBoxMACAddressSpec defines the desired state of a NetBox MAC address
            properties:
              comments:
                description: Comments
                nullable: true
                type: string
              description:
                description: Description
                nullable: true
                type: string
              interface:
                description: |-
                  Interface name (references NetBoxInterface CRD)
                  Format: "<device-name>/<interface-name>"
                type: string
              macAddress:
                description: 'MAC address (format: "aa:bb:cc:dd:ee:ff" or "aa-bb-cc-dd-ee-ff")'
                type: string
              tags:
                description: Tag references (references NetBoxTag CRDs)
                items:
                  description: |-
                    Kubernetes-compliant resource reference for NetBox CRDs

                    This follows the Kubernetes `TypedLocalObjectReference` pattern, which includes:
                    - `apiGroup`: The API group of the referenced resource (e.g., "dcops.microscaler.io")
                    - `kind`: The kind of the referenced resource (e.g., "NetBoxSite")
                    - `name`: The name of the referenced resource (required)
                    - `namespace`: The namespace of the referenced resource (optional, defaults to same namespace)

                    This enables Kubernetes to validate reference types and provides clear documentation
                    of what resource type is expected.
                  properties:
                    apiGroup:
                      description: API group of the referenced resource (e.g., "dcops.microscaler.io")
                      type: string
                    kind:
                      description: Kind of the referenced resource (e.g., "NetBoxSite", "NetBoxTenant")
                      type: string
                    name:
                      description: Name of the referenced NetBox CRD resource
                      type: string
                    namespace:
                      description: Namespace of the referenced resource (defaults to same namespace as the referencing resource)
                      nullable: true
                      type: string
                  required:
                  - apiGroup
                  - kind
                  - name
                  type: object
                nullable: true
                type: array
            required:
            - interface
            - macAddress
            type: object
          status:
            description: NetBoxMACAddressStatus defines the observed state of a NetBox MAC address
            nullable: true
            properties:
              error:
                description: Error message if reconciliation failed
                nullable: true
                type: string
              lastReconciled:
                description: Last reconciliation timestamp
                format: date-time
                nullable: true
                type: string
              netboxId:
                description: NetBox MAC address ID (set after creation)
                format: uint64
                minimum: 0.0
                nullable: true
                type: integer
              netboxUrl:
                description: NetBox MAC address URL
                nullable: true
                type: string
              state:
                description: Current state of the MAC address
                enum:
                - Pending
                - Created
                - Updated
                - Failed
                type: string
            required:
            - state
            type: object
        required:
        - spec
        title: NetBoxMACAddress
        type: object
    served: true
    storage: true
    subresources:
      status: {}
