# Dev edge — Envoy Gateway (shared-gitops-k8s-cluster stack envoy-gateway).
# Envoy does not consume networking.k8s.io Ingress; wiring is HTTPRoute + parentRefs.
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: pxe-server
  namespace: dcops-system
  labels:
    app: pxe-server
    app.kubernetes.io/part-of: dcops
  annotations:
    gitops.microscaler.io/hostname: pxe.dcops.dev.microscaler.local
    gitops.microscaler.io/gateway: microscaler-dev
spec:
  parentRefs:
    - name: microscaler-dev
      namespace: envoy-gateway-system
      sectionName: https
    - name: microscaler-dev
      namespace: envoy-gateway-system
      sectionName: http
  hostnames:
    - pxe.dcops.dev.microscaler.local
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: pxe-server
          port: 8080
